name: Chart README.md check
on: [push, pull_request]
jobs:
  lint-changelog:
    name: Lint changelog file
    runs-on: ubuntu-latest
    container:
      image: jnorwood/helm-docs:v0.14.0
      options: ci/test.sh
    steps:
    - name: Check out code
      uses: actions/checkout@v2
