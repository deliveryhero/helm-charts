apiVersion: v1
kind: Service
metadata:
  name: {{ template "resource_name" . }}
  labels:
    {{- include "labels" . | indent 4 }}
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "labels" . | indent 4 }}
