apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "toxiproxy.fullname" . }}-test-connection"
  labels:
{{ include "toxiproxy.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "toxiproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
