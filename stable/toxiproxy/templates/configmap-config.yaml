apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "toxiproxy.fullname" . }}-config
  labels:
{{ include "toxiproxy.labels" . | indent 4 }}
{{- if .Values.downscaler.disabled }}
data:
{{ (.Files.Glob "config/*").AsConfig | indent 2 }}
