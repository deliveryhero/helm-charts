apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dregsy.fullname" . }}
  labels:
    {{- include "dregsy.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{ toYaml .Values.config | indent 4 }}
