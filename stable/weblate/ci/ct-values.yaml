postgresql:
  fullnameOverride: weblate-postgres
  global:
    postgresql:
      auth:
        username: postgres
        password: weblate
        postgresPassword: weblate
        database: weblate
  primary:
    persistence:
      enabled: false

redis:
  fullnameOverride: weblate-redis
  auth:
    enabled: false
